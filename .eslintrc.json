module.exports = {
    // Çalışacağı ortamlar
    env: {
        browser: true,    // tarayıcı ortamı
        commonjs: true,   // CommonJS modül sistemi (require)
        es2021: true,     // ES2021 özellikleri
        node: true,       // Node.js ortamı
        jest: true        // Jest test framework
    },

    // Temel ESLint kurallar seti
    extends: "eslint:recommended",

    // JS sürümü / parser ayarları
    parserOptions: {
        ecmaVersion: "latest" // en güncel ECMAScript
    },

    // Kişisel kurallar
    rules: {
        "no-unused-vars": "warn", // kullanılmayan değişkenler uyarı
        "no-console": "off",      // console.log kullanımına izin
        "no-undef": "error"       // tanımsız değişken hatası ver
    }
};
